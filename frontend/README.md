## Steps to install tailwind with others libs

```
# Install Tailwind and others
npm install -D tailwindcss postcss autoprefixer

# Run command to init tailwind
npx tailwindcss init -p

```

# need to create a styles global;
